// import {provideHttpClient} from '.angular/c'

// export const HttpProviders = [
//     provideHttpClient(
//       // do this, to keep using your class-based interceptors.
//       withInterceptors([GarageInterceptorFn, LocalInterceptorFn])
//     ),
//   ];